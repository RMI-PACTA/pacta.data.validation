# pacta.data.validation

The `pacta.data.validation` R package provides a number of utility
functions to facilitate validation of data objects used in the PACTA
ecosystem of R packages.

## Installation

You can install the most recent version of pacta.data.validation from
[R-universe](https://r-universe.dev/) with:

``` r
install.packages("pacta.data.validation", repos = "https://rmi-pacta.r-universe.dev")
```

You can install the development version of pacta.data.validation from
[GitHub](https://github.com/) with:

``` r
# install.packages("devtools")
devtools::install_github("RMI-PACTA/pacta.data.validation")
```

## Usage

To validate a data object, pass it to the appropriate validation
function. For example…

``` r
library("pacta.data.validation")

masterdata_debt_datastore <- fake_masterdata_debt_datastore(id = "x")
validate_masterdata_debt_datastore(masterdata_debt_datastore)
```

# Package index

## All functions

- [`fake_abcd_flags_bonds()`](https://rmi-pacta.github.io/pacta.data.validation/reference/fake_abcd_flags_bonds.md)
  :

  Create an example `abcd_flags_bonds` object

- [`fake_abcd_flags_equity()`](https://rmi-pacta.github.io/pacta.data.validation/reference/fake_abcd_flags_equity.md)
  :

  Create an example `abcd_flags_equity` object

- [`fake_currencies()`](https://rmi-pacta.github.io/pacta.data.validation/reference/fake_currencies.md)
  :

  Create an example `currencies` object

- [`fake_financial_data()`](https://rmi-pacta.github.io/pacta.data.validation/reference/fake_financial_data.md)
  :

  Create an example `financial_data` object

- [`fake_intermediate_scenario_data()`](https://rmi-pacta.github.io/pacta.data.validation/reference/fake_intermediate_scenario_data.md)
  :

  Create an example `intermediate_scenario_data` object

- [`fake_masterdata_debt_datastore()`](https://rmi-pacta.github.io/pacta.data.validation/reference/fake_masterdata_debt_datastore.md)
  :

  Create an example `masterdata_debt_datastore` object

- [`fake_masterdata_ownership_datastore()`](https://rmi-pacta.github.io/pacta.data.validation/reference/fake_masterdata_ownership_datastore.md)
  :

  Create an example `masterdata_ownership_datastore` object

- [`is_valid_isin()`](https://rmi-pacta.github.io/pacta.data.validation/reference/is_valid_isin.md)
  : Validate a vector of ISINs

- [`validate_abcd_flags_bonds()`](https://rmi-pacta.github.io/pacta.data.validation/reference/validate_abcd_flags_bonds.md)
  :

  Validate a `abcd_flags_bonds` object

- [`validate_abcd_flags_equity()`](https://rmi-pacta.github.io/pacta.data.validation/reference/validate_abcd_flags_equity.md)
  :

  Validate a `abcd_flags_equity` object

- [`validate_currencies()`](https://rmi-pacta.github.io/pacta.data.validation/reference/validate_currencies.md)
  :

  Validate a `currencies` object

- [`validate_financial_data()`](https://rmi-pacta.github.io/pacta.data.validation/reference/validate_financial_data.md)
  :

  Validate a `financial_data` object

- [`validate_intermediate_scenario_output()`](https://rmi-pacta.github.io/pacta.data.validation/reference/validate_intermediate_scenario_output.md)
  : Validate an intermediate scenario object

- [`validate_masterdata_debt_datastore()`](https://rmi-pacta.github.io/pacta.data.validation/reference/validate_masterdata_debt_datastore.md)
  :

  Validate a `masterdata_debt_datastore` object

- [`validate_masterdata_ownership_datastore()`](https://rmi-pacta.github.io/pacta.data.validation/reference/validate_masterdata_ownership_datastore.md)
  :

  Validate a `masterdata_ownership_datastore` object

