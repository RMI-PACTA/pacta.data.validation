# Validate a `financial_data` object

This function validates that an object is a valid `financial_data`
dataset.

## Usage

``` r
validate_financial_data(data)
```

## Arguments

- data:

  An object (typically a data frame)

## Value

`TRUE` if the object is valid, otherwise an error with a message
explaining the failed assertions
