# Validate a `abcd_flags_bonds` object

This function validates that an object is a valid `abcd_flags_bonds`
dataset.

## Usage

``` r
validate_abcd_flags_bonds(data)
```

## Arguments

- data:

  An object (typically a data frame)

## Value

`TRUE` if the object is valid, otherwise an error with a message
explaining the failed assertions
